<div class="row">
  <div class="col-sm-4 col-xs-12 borderme">
    <!-- <h3 class="<%= topic_area_slug %> <%= subject_slug %>" id="<%= indicator.slug %>"> -->
    <p>
      <%= link_to indicator.title, indicator_path(indicator) %>
    </p>
    <!-- </h3> -->
  </div>

  <div class="col-sm-8 col-xs-12 otherborderme">
    <%= render partial: 'topic_areas/status', locals: { indicator: indicator } %>
  </div>
</div>